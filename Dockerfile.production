# =============================================================
# Stage 1 — Composer dependencies (no-dev, optimised)
# =============================================================
FROM composer:2.8 AS vendor

WORKDIR /app

COPY composer.json composer.lock ./

# --ignore-platform-reqs: composer image lacks the PHP extensions.
# They are already installed in the shared base image (Stage 3).
RUN composer install \
    --no-dev \
    --no-interaction \
    --no-scripts \
    --no-autoloader \
    --prefer-dist \
    --ignore-platform-reqs

# =============================================================
# Stage 2 — Frontend assets
# =============================================================
FROM node:22-alpine AS assets

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
COPY --from=vendor /app/vendor ./vendor

RUN npm run build

# =============================================================
# Stage 3 — PHP-FPM runtime
#
# Uses the shared base image — all PHP extensions are pre-installed.
# This eliminates the 2-3 minute compilation step on every build.
# Base image: ghcr.io/godiah/php-base:8.4-fpm-alpine
# Source: https://github.com/godiah/laravel-cicd/tree/main/base-image
#
# NOTE: The php-base package must be set to Public visibility on GHCR
# so GitHub Actions can pull it without extra credentials:
#   GitHub → godiah → Packages → php-base → Package Settings → Change visibility → Public
# =============================================================
FROM ghcr.io/godiah/php-base:8.4-fpm-alpine AS production

LABEL maintainer="ModelHub"

WORKDIR /var/www/html

# ---- Application files ----
COPY --from=vendor /app/vendor ./vendor
# Bring composer binary so we can run dump-autoload with source files present
COPY --from=vendor /usr/bin/composer /usr/local/bin/composer
COPY . .

COPY --from=assets /app/public/build ./public/build

# Generate optimised classmap now that app/ and all source dirs are present
RUN composer dump-autoload --optimize --no-dev --no-scripts \
    && rm /usr/local/bin/composer

# ---- Storage structure ----
# Git does not track empty directories — ensure Laravel's required paths exist.
# Must exist before any `artisan` call below — bootstrap/cache is where the
# framework writes its compiled service-container cache, even for a command
# as simple as storage:link.
RUN mkdir -p \
        storage/framework/views \
        storage/framework/cache/data \
        storage/framework/sessions \
        storage/framework/testing \
        storage/logs \
        bootstrap/cache

# public/storage -> storage/app/public, baked in at build time. The Stage 4
# nginx image copies public/ as a static snapshot with no entrypoint script
# of its own, so this must exist before that COPY — `docker build` preserves
# symlinks across COPY --from, and at runtime both the app and nginx
# containers mount the same shared `storage` named volume at this exact
# path, so the symlink resolves correctly in both. Without this, every
# uploaded/seeded public file 404s through nginx even though it's on disk.
RUN php artisan storage:link

# Remove dev/test directories not needed at runtime
# Note: Alpine sh does not support brace expansion — list paths explicitly
RUN rm -rf tests/ .github/ docker/

# ---- Permissions ----
RUN chown -R www-data:www-data \
        storage/ \
        bootstrap/cache/ \
    && chmod -R 775 \
        storage/ \
        bootstrap/cache/

EXPOSE 9000

# =============================================================
# Stage 4 — Nginx (static assets baked in, no PHP)
# =============================================================
FROM nginx:1.27-alpine AS web

COPY docker/nginx/nginx.conf /etc/nginx/nginx.conf
COPY docker/nginx/conf.d/app.conf /etc/nginx/conf.d/default.conf

COPY --from=production /var/www/html/public /var/www/html/public

EXPOSE 80
